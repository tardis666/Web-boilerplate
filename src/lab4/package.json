{
  "name": "site",
  "version": "1.0.0",
  "description": "666",
  "scripts": {
    "server": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.0"
  }
}
